# Empty uses /api on the same origin and Vite's development proxy.
VITE_API_BASE_URL=
API_PROXY_TARGET=http://127.0.0.1:8000
# Must match API_WRITE_TOKEN if configured. A browser token is not user authentication.
VITE_API_WRITE_TOKEN=
